import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Accés — Tràmit Economistes',
}

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
